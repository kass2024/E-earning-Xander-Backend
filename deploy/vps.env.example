# Local automation settings (copy to vps.env — never commit vps.env)
VPS_HOST=root@YOUR_VPS_IP
VPS_PATH=/opt/e-learning-xander
# Set to 1 to import deploy/db/latest.sql.gz on next remote deploy
IMPORT_DB=1
# Optional SSH key: C:\Users\user\.ssh\id_rsa
# SSH_KEY=
